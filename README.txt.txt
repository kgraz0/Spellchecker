Compile soundex.java and then compile Spellchecker.java, then run Spellchecker to begin.

The input that is used to detect miss-spelled words is input.txt file.

The dictionary where miss-spelled words are cross-checked is dictionary.txt file.

The output where the correctly spell checked file is written to is output.txt file.